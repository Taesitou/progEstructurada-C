#include <stdio.h>

int main() {
	
	/*
	fgetc retorna el caracter leido, 
	
	RETORNA -1 cuando encuentra el final del archivo (End of file) EOF  
	
	NULL = None de py
	1 byte de 0 a 255
	busca en el asciiu
	
	fgetc cuando tengo distintos strings con distintos tamaños porque fgets define el tamaño entonces no voy a poder leer todos
	
	*/
	
	
	return 0;
}

