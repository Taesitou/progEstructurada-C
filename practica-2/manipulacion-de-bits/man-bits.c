#include <stdio.h>

int main(int argc, char *argv[]) {
	// 256 valores 8 bits -> 8 celdas de memoria
	/*
	~ not		NOT(x) = y
	
	& and		AND(a,b) = ___________
	| or		OR(a,b)=
	^xor 		XOR(a,b) =  				(tiene que haber un solo 1 para que resulte 1)
	
	*/
	return 0;
}

